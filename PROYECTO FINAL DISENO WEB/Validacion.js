function validar(){

    var admin = "admin";
    var user = "user";

    if(document.Forms.admin == admin){
        document.Form.txtusuario.focus()
    }

    if(document.Forms.user == user){
        document.Form.txtusuario.focus() 
    }
}